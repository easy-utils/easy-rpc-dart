// easy-rpc Dart — Flutter/mobile client transports (see README below).
//
// This directory holds Cronet (Android), Cupertino (iOS/macOS) and Fetch (Web)
// Transport implementations. They depend on Flutter SDK packages
// (`cronet_http` / `cupertino_http`), so the pure Dart VM build excludes them
// via `analysis_options.yaml`. When used in a Flutter app, import these to get
// system-level HTTP/3 (Cronet / NSURLSession / browser fetch).
